<div class="jumbotron">
    <h1>Node EJS</h1>
    <p>Vamos aprender a trabalhar com arquivos EJS</p>
</div>

<%= subtitle %>

    <ul>
        <% items.forEach(function show(item){ %>
            <li>
                <strong>
                    <%= item.title%>
                </strong>
                <%=item.message %>
            </li>
            <% }) %>

    </ul>